just passes env object, it isn't related to process.env.

```
pass-arguments
```

dotenv-webpack - This plugin is to read the environment variables from the .env file securely by only exposing the variables used in the code
