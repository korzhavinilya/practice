query {
  getAllStudents {
    _id, firstName, lastName
  }
}

mutation {
  createStudent(input: {
    firstName: "Vitali",
    lastName: "Gogi",
    gender: "m"
  }) {
    _id, firstName, lastName
  }
}

